- name: Open firewall
  include: firewall.yml
  vars:
    port: "{{ item }}"
  loop:
    - ftp
    - ftps
- name: Make sure vsftpd is running
  systemd:
    name: vsftpd
    daemon_reload: yes
    enabled: yes
    state: restarted
