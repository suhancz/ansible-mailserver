$TTL 2h;
$ORIGIN {{ mailserver_domain }}.
@          SOA ns.{{ mailserver_domain }} hostmaster.{{ mailserver_domain }} 0 10800 3600 604800 3600 ( 1 12h 15m 3w 2h)
           NS ns.{{ mailserver_domain }}. 
; begin RPZ RR definitions

{% for custom_domain in [mailserver_domain] + custom_domains %}
{{ custom_domain }} A {% if wireguard is defined %}{{ wireguard.server.cidr|split('/')|first }}{% else %}127.0.0.1{% endif %}
*.{{ custom_domain }} A {% if wireguard is defined %}{{ wireguard.server.cidr|split('/')|first }}{% else %}127.0.0.1{% endif %}
{% endfor %}
